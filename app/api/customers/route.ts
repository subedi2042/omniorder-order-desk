import { env } from "cloudflare:workers";
import { requireSalesUser } from "../_auth";

async function ensureTable() { await env.DB.prepare("CREATE TABLE IF NOT EXISTS customers (id TEXT PRIMARY KEY, business TEXT NOT NULL, contact TEXT NOT NULL, email TEXT NOT NULL, phone TEXT NOT NULL, address TEXT NOT NULL, updated_at INTEGER NOT NULL)").run(); }
export async function GET(request: Request) { if (!await requireSalesUser(request)) return Response.json({ error: "Unauthorized" }, { status: 401 }); await ensureTable(); const { results } = await env.DB.prepare("SELECT id, business, contact, email, phone, address FROM customers ORDER BY business").all(); return Response.json({ customers: results }); }
export async function POST(request: Request) { if (!await requireSalesUser(request)) return Response.json({ error: "Unauthorized" }, { status: 401 }); const { customers } = await request.json() as { customers: Array<{ id: string; business: string; contact: string; email: string; phone: string; address: string }> }; await ensureTable(); await env.DB.batch(customers.map((customer) => env.DB.prepare("INSERT INTO customers (id,business,contact,email,phone,address,updated_at) VALUES (?,?,?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET business=excluded.business,contact=excluded.contact,email=excluded.email,phone=excluded.phone,address=excluded.address,updated_at=excluded.updated_at").bind(customer.id, customer.business, customer.contact, customer.email, customer.phone, customer.address, Date.now()))); return Response.json({ saved: customers.length }); }
